# Logged-in Chrome sessions / profiles — credentials/cookies. NEVER commit.
chrome-profile/
.chrome_ds4/

# Local harness config
.claude/

# Downloaded résumés + per-vacancy data — candidate PII. NEVER commit.
data/

# Archived earlier code + old CV dumps (PII, large). Not part of the live tool.
old_algo/

# Build/throwaway
hrstuff-code.zip
__pycache__/
*.pyc
.DS_Store
